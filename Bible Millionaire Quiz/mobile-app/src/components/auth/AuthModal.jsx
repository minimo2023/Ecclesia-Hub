// Desktop and mobile intentionally share one authentication implementation so
// Google, email verification, recovery, and accessibility cannot drift apart.
export { default } from '../../../../src/features/auth/AuthModal.jsx';
