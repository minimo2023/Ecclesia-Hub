# Ecclesia Hub 資料夾整理與清理報告

## 完成時間

2026-08-25（Asia/Taipei）

## 整理後定義

外層 `D:\Ecclesia-Hub` 只保留兩個中文分類目錄：`開發/` 與 `舊檔/`。`D:\Ecclesia-Hub\開發` 是唯一可建置、測試與部署的開發根目錄；`D:\Ecclesia-Hub\舊檔` 不參與任何開發流程。

### 開發區

- `Bible Millionaire Quiz/`
- `steward-ops/`
- `platform/`
- `data/`
- `uploads/`
- `doc/`
- `node_modules/`
- 根目錄的 package、Docker、nginx 與現行 `.env*`

### 舊檔區

- `D:\Ecclesia-Hub\舊檔\部署驗證\2026-08-25\`：由整理後原始碼重新建置的部署包。
- `D:\Ecclesia-Hub\舊檔\資料庫快照\`：本輪生產等價驗證使用的 PostgreSQL 快照。

## 已清除

- `_recovery`、`_candidate-validation`、`_production-canonical` 的重複完整快照與候選容器資料。
- 候選 Docker 容器、network 與專用 PostgreSQL volume。
- `.codex-deploy-auto-*` 、`deploy-v4-newgen` 與舊部署修補包。
- 根目錄重複的 `assets`、`src`、`scripts`、空的 `mobile-app`、`static_assets` 與臨時 Docker 設定。
- 舊 SQL/dump/CSV、除錯程式、一次性 migration 腳本、過期 `.env.bak` 與重審日誌。
- XIT 根目錄中已完成使命的 patch/fix、state dump、old HTML/CSS 與 scratch 檔案。
- 作用中專案內的 legacy 腳本與 Vite 臨時快取。

合計約清除 3.6 GiB 的重複、舊版或可重建內容。這些已刪內容不在專案內保留，需要時只能從 ProjectGuardian 或其他外部備份復原。

## 保留與精簡

- 正式 `.env`、`.env.production` 與範例設定均保留。
- `data/` 仍保留可能供本機相容、資料遷移與經文來源使用的 SQLite、註釋、地理與 raw 資料；只刪除明確過期的 SQL 副本與日誌。
- 生產後端不變參考由完整 164 MiB 副本精簡為 `platform/production-reference/server-index-20260825.js`。
- 保留的 dump 與部署包均有 SHA-256 側車檔。

## 整理後驗證

- 生產桌機、手機與後端基準驗證通過。
- 可開發基準驗證通過。
- 後端測試 149/149 通過。
- XIT 測試 121/121 通過。
- 桌機與手機建置通過。
- lint、UTF-8、Node 語法、XIT 資料完整性與正式相依漏洞稽核通過。
- `npm run deploy:package` 能從整理後開發根目錄成功建置部署包。

最新部署包 SHA-256：

`ecb66fcd01e90c5b1625e46022bb264786d08be4b976def2bd50df904a4c38e9`
